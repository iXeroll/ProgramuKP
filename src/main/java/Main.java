import java.io.IOException;
import java.util.Scanner;

public class Main {

    /*public static void main(String[] args) throws IOException {
        Registration registration = new Registration();

        Scanner sc = new Scanner(System.in);

        String regName = registration.registrationName(sc);
        String regUsername = registration.registrationUsername(sc);
        String regPasswrod = registration.registrationPassword(sc);
        String regConfirmPassword = registration.registrationRepeatedPassword(sc);

        registration.addUserToAnExitingList(regName, regUsername, regPasswrod);
    }*/
}
